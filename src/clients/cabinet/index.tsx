import ReactDOM from 'react-dom';
import React from 'react';

const App = () => <h1>Cabinet</h1>;

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
